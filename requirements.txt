# Jupyter Book 1.x（使用 _config.yml + _toc.yml 构建）
jupyter-book>=0.15,<2

# 示例 intro.ipynb 中 matplotlib 绘图所需
matplotlib>=3.5.0
numpy>=1.20

# Textbook PDF split tool (process_pdf, MCP server)
PyMuPDF>=1.24
mcp>=1.0

# Subfigures (side-by-side images with captions)
sphinx-subfigure>=0.2

# Video embedding with controls, loop, etc.
sphinxcontrib-video>=0.4

# Interactive widgets (Project: 32-8 hysteresis)
ipywidgets>=8.0
nest-asyncio>=1.5

# Ising simulation (test.ipynb)
torch>=2.0
numba>=0.58
