# Jupyter Book 1.x (build with _config.yml + _toc.yml)
jupyter-book>=0.15,<2

# Notebooks: matplotlib, numpy, scipy (32-8 ferromagnetism, etc.)
matplotlib>=3.5.0
numpy>=1.20
scipy>=1.9

# Textbook PDF split tool (process_pdf, MCP server)
PyMuPDF>=1.24
mcp>=1.0

# Sphinx extensions (Jupyter Book build)
sphinx-subfigure>=0.2
sphinxcontrib-video>=0.4

# Interactive widgets (32-8 ferromagnetism)
ipywidgets>=8.0

# 32-8 Ising simulation (numba JIT)
numba>=0.58

# FaceCroper (assets/img/photos/_maker/)
torch>=2.0
