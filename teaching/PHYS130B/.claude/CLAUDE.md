# PHYS130B — Quantum Mechanics Lecture Notes

## Project Overview

Jupyter Book lecture notes for UCSD PHYS 130B (Quantum Mechanics), taught by Prof. Yi-Zhuang You. Textbook: David Tong, *Quantum Mechanics* (Cambridge University Press).

The notes live in `notes_src/` as `.ipynb` notebooks compiled to a static HTML site via `jupyter-book build`. The published site is served at `https://everettyou.github.io/teaching/PHYS130B/notes/`.

## Course Structure

6 chapters, 21 sections, 63 subsections = **84 notebooks** in a 3-level hierarchy:

| Chapter | Topic | Sections | Subsections | Textbook |
|---------|-------|----------|-------------|----------|
| Ch1 | Qubit | 3 | 9 | §4 |
| Ch2 | Identical Particles | 3 | 9 | §8 |
| Ch3 | Path Integral | 4 | 12 | §15 |
| Ch4 | Phase and Gauge | 5 | 15 | §9, §11 |
| Ch5 | Perturbation Theory | 2 | 6 | §10, §11 |
| Ch6 | Quantum Foundations | 4 | 12 | §16 |

## Key Files

- `notes_src/` — Source notebooks and config
- `notes_src/_toc.yml` — Table of contents (defines chapter/section/subsection hierarchy)
- `notes_src/_config.yml` — Jupyter Book config
- `notes_src/index.md` — Landing page with master TOC table
- `notes_src/ch{N}_*/index.md` — Chapter landing pages with section tables and glossary
- `notes/` — Built HTML output (generated by `build.sh`, do not edit)
- `build.sh` — Build script: clears `_build/`, runs `jupyter-book build`, syncs to `notes/`, rewrites `_static` → `static` for GitHub Pages

## Before You Start

Read these companion docs in this directory:
- **`design.md`** — Teaching philosophy, notebook conventions, cell structure, MyST markdown rules, admonition classes, eigenstate notation
- **`experience.md`** — Critical lessons about ipynb file format, corruption patterns, and safe editing practices
- **`progress.md`** — Current improvement progress tracker (read before starting, update after finishing)

**IMPORTANT**: The teaching philosophy in `design.md` §"Teaching Philosophy: Physics Education in the AI Era" is the guiding vision for all work on these notes. Read it carefully before making any content decisions.

## Critical Rules

1. **Never use `NotebookEdit` on these notebooks.** It causes char-by-char corruption. Use Python `json.load`/`json.dump` via Bash instead. See `experience.md` for details.

2. **Never use `Edit` on `.ipynb` files.** It doesn't work on JSON notebook format.

3. **Always ensure `\n` terminators** on every non-final line in cell `"source"` arrays. Without them, Jupyter Book concatenates lines and rendering breaks.

4. **Always `Read` before writing.** Inspect the notebook's current state before modifying. Verify cell indices haven't shifted.

5. **Validate after bulk edits.** Run the validation script in `experience.md` §4 to catch corruption before building.

6. **Clear `_build/` before rebuilding.** Stale cached copies in `_build/html/_sources/` and `_build/jupyter_execute/` persist across incremental builds.

## Build

From the `PHYS130B/` directory:
```bash
./build.sh
```
This clears `_build/`, builds the book, syncs to `notes/`, and patches paths for GitHub Pages.

For local preview, serve `notes/` with any static server (e.g., `python -m http.server 4000 -d notes`).

## Lecture Note Refactoring Plan

The notes are being refactored to support AI-era pedagogy. This is a multi-session effort tracked in `progress.md`. The work is organized into **workstreams**, each with specific deliverables.

### Workstream 1: Prompt Review (63 notebooks)

Each x.y.z notebook already has a Prompts box (cell 1) with guiding questions. These must be reviewed to ensure:
- Questions help students preview the lecture content effectively with AI
- Questions are phrased naturally (readable by humans) while being specific enough for AI to give high-quality answers
- Questions cover the key concepts that will appear in the lecture
- At least one question pushes beyond the basics — something AI might struggle with or give an incomplete answer to
- Do NOT do heavy prompt engineering — the prompts must read like a professor's study guide, not like API prompts

**Status**: Review each notebook's cell 1, flag inadequate prompts, rewrite where needed.

### Workstream 2: Homework (63 notebooks)

Add a `## Homework` section (new cell 3) to each x.y.z subsection notebook. Requirements per `design.md`:
- 5–10 problems per subsection
- Each problem targets one concept or skill
- Mix of: conceptual questions, short derivations, "show that" proofs, physical reasoning, simple applications
- Avoid tedious numerical computation or trick combinations
- Problems should be reproducible on an in-person exam (no AI needed if student truly understood)
- Difficulty: standard undergraduate quantum mechanics level

**Status**: Create homework for each subsection. Physics accuracy is critical.

### Workstream 3: Projects (21 section notebooks)

Restructure each x.y section parent notebook to the new format (per `design.md`):
- `## Overview` with a motivating paragraph first
- `### Topics` table, `### Key Concepts`, `### Learning Objectives`
- `## Projects` with 2–3 research project ideas per section

Project design requirements:
- Require genuine scientific inquiry (not just calculation)
- Achievable in ~2 weeks with AI assistance
- Categories: simulation, comprehensive study, research investigation
- Each specifies: objective, suggested approach, expected deliverable

**Status**: Restructure all 21 section parents and add projects.

### Workstream 4: Discussion Problems (63 notebooks)

Insert `:::{admonition} Discussion\n:class: warning` boxes within lecture content (cell 2) at natural conceptual junctures. Requirements:
- Problems must be genuinely debatable — not just hard, but *controversial* or conceptually subtle
- Targets conceptual barriers where discussion genuinely helps
- Should provoke students to check AI answers against physical reasoning
- 1–3 discussion boxes per subsection, placed inline where they arise naturally

**Status**: Design and insert discussion problems.

### Workstream 5: Content Polish (84 notebooks)

Refactor existing lecture content to serve its new role as authoritative reference:
- Key definitions, results, and equations are visible and prominent (not hidden)
- Extended derivations and pedagogical narrative go in collapsible dropdown boxes
- Notes should be clean, inviting, structurally precise
- Remove wall-of-text; ensure every section has clear visual hierarchy
- The notes serve as a "knowledge graph skeleton" that AI agents will teach from

**Status**: Review and polish each notebook.

### Workstream 6: Validation

After each batch of changes:
- Run the corruption validation script (experience.md §4)
- Verify notebook cell structure matches design.md
- Spot-check physics accuracy
- Professor proofreads rendered output after `./build.sh`

## Multi-Agent Workflow

This project uses a scheduled continuous-improvement loop. The workflow operates as follows:

### Agent Roles

1. **Manager Agent** (scheduled, runs every 10–15 min): Reads `progress.md`, checks what work is pending, dispatches tasks to specialized agents. Also incorporates professor's feedback from `feedback.md`.

2. **Review Agent**: Reads current notebooks and proposes specific improvements. Outputs proposals to `progress.md` under the relevant workstream.

3. **Design Agent**: Designs homework problems, project ideas, and discussion questions. Requires deep understanding of the physics content. Outputs draft content for the Writer Agent.

4. **Writer Agent**: Implements designs into notebooks using proper ipynb JSON format. Follows all rules in `experience.md` strictly. Always validates after writing.

5. **Science Agent**: Reviews written content for physics correctness. Checks equations, notation, conceptual accuracy. Flags errors for the Writer Agent to fix.

6. **Professor** (human): Runs `./build.sh`, proofreads rendered output, writes feedback to `feedback.md`. The Manager Agent picks up feedback and dispatches corrections.

### Progress Tracking

`progress.md` is the single source of truth. Format:

```markdown
## Workstream N: [Name]

### Completed
- [x] notebook-name.ipynb — [date] — [what was done]

### In Progress
- [ ] notebook-name.ipynb — [agent working on it] — [status]

### Pending
- [ ] notebook-name.ipynb

### Issues
- notebook-name.ipynb: [description of problem found]
```

### Feedback Loop

`feedback.md` is where the professor writes comments:

```markdown
## [Date]
### [notebook or general]
- [feedback item]
```

The Manager Agent reads this at the start of each run and creates tasks from it.

### Dispatch Rules

- Only ONE agent writes to a given notebook at a time (no parallel writes to same file)
- Always read the notebook before writing
- Always validate after writing
- Always update `progress.md` after completing work
- If a workstream has dependencies (e.g., Homework depends on Lecture Notes being finalized), respect the ordering
- Prefer depth over breadth: finish one notebook completely before moving to the next
