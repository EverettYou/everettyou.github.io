---
layout: default
---

<article>
  <section class="page-content">
    {% comment %}
      Curriculum Vitae Layout
      Reads CV content from page front matter and content
    {% endcomment %}

    <div class="cv-container">
      <div class="cv-header">
        <div class="cv-header-content">
          <div class="cv-photo">
            <img src="{{ site.baseurl }}/assets/img/photos/YZYou2.png" alt="{{ page.cv_name | default: 'Name' }}">
          </div>
          <div class="cv-header-right">
            <h1 class="cv-name"><strong>{{ page.cv_name | default: "Name" }}, Ph. D.</strong></h1>
            <div class="cv-contact">
              {% for item in site.data.social.cv %}
                {% assign key = item[0] %}
                {% assign value = item[1] %}
                {% if value %}
                  {% assign info = site.data.info[key] %}
                  {% assign builder = site.data.builder[key] %}
                  {% if builder %}
                    {% if builder.label %}
                      {% assign label = builder.label %}
                    {% else %}
                      {% assign label = key | capitalize %}
                    {% endif %}
                    {% if builder.type == "link" %}
                      {% if key == "website" %}
                        {% assign url = site.data.site.url | default: site.url %}
                        {% assign info = url %}
                      {% else %}
                        {% assign url = builder.pre | append: info | append: builder.pos %}
                      {% endif %}
                      <div class="cv-contact-line">
                        <strong>{{ label }}</strong>:
                        <a href="{{ url }}" target="_blank">{{ info }}</a>
                      </div>
                    {% elsif builder.type == "text" %}
                      <div class="cv-contact-line">
                        <strong>{{ label }}</strong>:
                        {{ info }}
                      </div>
                    {% endif %}
                  {% endif %}
                {% endif %}
              {% endfor %}
            </div>
          </div>
        </div>
      </div>

      {% comment %} 
        Render the content - Jekyll has already converted Markdown to HTML
        We'll wrap it in a div and let CSS handle the styling
      {% endcomment %}
      <div class="cv-content-wrapper">
        {{ content }}
      </div>

      {% comment %} Certification section - always at the end with gray text {% endcomment %}
      <div class="cv-certification">
        <p>I hereby certify that the above information is true and correct to the best of my knowledge and belief.</p>
        <p>{{ page.cv_name }}</p>
        <p>{{ page.cv_date }}</p>
      </div>
    </div>
  </section>
</article>
